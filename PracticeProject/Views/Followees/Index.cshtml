@model IEnumerable<PracticeProject.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
}

<h1>Who I follow</h1>

<ul>
    @foreach (var artist in Model)
    {
        <li>@artist.Name</li>
    }
</ul>

